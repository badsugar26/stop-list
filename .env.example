# Порт бэкенда
PORT=3000

# Окружение: development | production
NODE_ENV=development

# URL клиента — нужен для CORS на бэке
CLIENT_URL=http://localhost:5173

# URL API — используется фронтом (Vite подставит через import.meta.env)
VITE_API_URL=http://localhost:3000